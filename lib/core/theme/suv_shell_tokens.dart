abstract final class SuvShellTokens {
  // Vertical breathing room above tab bars/rails for the floating mini-player.
  static const double miniDockGap = 8;

  // Horizontal card padding envelope contributes to practical dock reserve.
  static const double miniDockEnvelope = 8;

  // Android shell uses a tighter fixed offset above NavigationBar.
  static const double androidMiniBottom = 12;

  // iPhone floating create action spacing above mini-player.
  static const double iPhoneFabGap = 76;
}
