# Fireball iOS Native

Swift + SwiftUI app mirroring Android feature parity with adaptive iPhone/iPad UI and Fireball backend contracts.

Current foundation includes:

- Fireball core models and settings schema in Swift.
- API client scaffolding for iTunes/Invidious/LRCLIB.
- Adaptive navigation shell (`TabView` on compact, `NavigationSplitView` on regular width).
